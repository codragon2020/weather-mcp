{
	"name": "weather-mcp",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "An MCP (Model Context Protocol) server exposing a get_weather tool via stdio.",
	"main": "dist/server.js",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"test:mcp": "tsx src/testClient.ts",
		"start": "node dist/server.js",
		"dev": "tsx src/server.ts"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.25.2",
		"zod": "^4.3.5"
	},
	"devDependencies": {
		"@types/node": "^22.10.10",
		"tsx": "^4.19.2",
		"typescript": "^5.7.3"
	},
	"engines": {
		"node": ">=18"
	}
}
